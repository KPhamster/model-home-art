import { notFound } from "next/navigation";

export default function GalleryPage() {
  // Gallery is intentionally disabled for now.
  // Restore by renaming page.disabled.tsx back to page.tsx.
  notFound();
}
